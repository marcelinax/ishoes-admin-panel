import { Navbar } from '../components/compositional/Navbar';
import React from 'react';

export const DefaultLayout = () => {
    return (
        <div>
            <Navbar/>
        </div>
    );
};
